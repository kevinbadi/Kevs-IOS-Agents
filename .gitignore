node_modules/
dist/
.env
.env.*
!.env.example
devices.json
.wda/
.scheduler-data/
static/assets/
*.log
.appium2/
logs/

# Cold DM lead contact state (per-run outcomes; keep local)
data/leads/state/

# Hinge shadow-mode captures (screenshots of other people; keep local)
data/hinge/
# LinkedIn tour captures
data/linkedin/
# Agent mode run captures (screenshots of the phone; keep local)
data/agent/
